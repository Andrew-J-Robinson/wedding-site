{
  "name": "wedding-site",
  "version": "1.0.0",
  "description": "Responsive wedding website with RSVP and admin dashboard",
  "scripts": {
    "migrate": "node --env-file=.env scripts/run-migrations.js",
    "test": "node --test",
    "test:watch": "node --test --watch"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.99.3",
    "fuse.js": "^6.6.2",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "pg": "^8.20.0"
  }
}
